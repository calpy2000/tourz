// On-screen version badge (top-left of every page, see App.jsx) — same convention as
// tools/poi-field-review's own version badge. No git tags; bump this string by hand on every
// real functional deploy (data or behavior change) so a fresh screenshot of the live app tells
// you which build you're looking at.
export const APP_VERSION = 'V1.8'
